import { DrawareaDirective } from './drawarea.directive';

describe('DrawareaDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawareaDirective();
    expect(directive).toBeTruthy();
  });
});
